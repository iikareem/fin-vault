services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: family
      POSTGRES_PASSWORD: family
      POSTGRES_DB: fin_vault
    ports:
      - "5433:5432"
    volumes:
      - fin_vault_pg:/var/lib/postgresql/data

volumes:
  fin_vault_pg:
