/**
 * Product feature flags (compile-time). Flip to re-enable house books UI.
 * House data still exists in the API; this only hides it from the app shell.
 */
export const HOUSE_BOOKS_ENABLED = false;
